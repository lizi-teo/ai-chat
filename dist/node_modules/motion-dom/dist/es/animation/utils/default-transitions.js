import { transformProps as e } from "../../render/utils/keys-transform.js";
//#region node_modules/motion-dom/dist/es/animation/utils/default-transitions.mjs
var t = {
	type: "spring",
	stiffness: 500,
	damping: 25,
	restSpeed: 10
}, n = (e) => ({
	type: "spring",
	stiffness: 550,
	damping: e === 0 ? 2 * Math.sqrt(550) : 30,
	restSpeed: 10
}), r = {
	type: "keyframes",
	duration: .8
}, i = {
	type: "keyframes",
	ease: [
		.25,
		.1,
		.35,
		1
	],
	duration: .3
}, a = (a, { keyframes: o }) => o.length > 2 ? r : e.has(a) ? a.startsWith("scale") ? n(o[1]) : t : i;
//#endregion
export { a as getDefaultTransition };
