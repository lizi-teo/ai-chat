import { statsBuffer as e } from "../../stats/buffer.js";
import { activeAnimations as t } from "../../stats/animation-count.js";
import { mapEasingToNativeEasing as n } from "./easing/map-easing.js";
//#region node_modules/motion-dom/dist/es/animation/waapi/start-waapi-animation.mjs
function r(r, i, a, { delay: o = 0, duration: s = 300, repeat: c = 0, repeatType: l = "loop", ease: u = "easeOut", times: d } = {}, f = void 0) {
	let p = { [i]: a };
	d && (p.offset = d);
	let m = n(u, s);
	Array.isArray(m) && (p.easing = m), e.value && t.waapi++;
	let h = {
		delay: o,
		duration: s,
		easing: Array.isArray(m) ? "linear" : m,
		fill: "both",
		iterations: c + 1,
		direction: l === "reverse" ? "alternate" : "normal"
	};
	f && (h.pseudoElement = f);
	let g = r.animate(p, h);
	return e.value && g.finished.finally(() => {
		t.waapi--;
	}), g;
}
//#endregion
export { r as startWaapiAnimation };
